{
  "name": "nba-career-map-data-proxy",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker: Origin-gated proxy for the NBA Career Map site's JSON data files.",
  "scripts": {
    "dev": "wrangler dev --local",
    "dev:test": "wrangler dev --local --config wrangler.test.toml",
    "deploy": "wrangler deploy",
    "test": "node test/proxy.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  }
}
