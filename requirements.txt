# The updater uses only the Python standard library (urllib, json, re,
# argparse, datetime) so there are no runtime dependencies to install.
# This file is intentionally empty of hard requirements; it exists so the
# GitHub Actions workflow's `pip install -r requirements.txt` step is a no-op
# that still documents the (lack of) dependencies.
